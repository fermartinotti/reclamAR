language: java
sudo: required
jdk:
  - oraclejdk8

addons:
    apt:
    packages:
    - oracle-java8-installer

    script: 
  - cd backend
  - mvn clean install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8